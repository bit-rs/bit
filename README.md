### 🛰️ Bit
... is Low-level programming language for building efficient software.

### Examples

```bit
use std/io as io

fn main() {
    io.println("Hello, world!");
}
```

```bit
struct House {
  street: u16,
  id: u64,
}
```
