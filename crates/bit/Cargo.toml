[package]
name = "bit"
version = "0.1.0"
edition = "2024"

[dependencies]
camino = "1.2.2"
bit_rt = { path = "../bit_rt" }
bit_common = { path = "../bit_common" }
miette = { git = "https://github.com/bit-rs/miette", features = ["fancy"] }
clap = { version = "4.5.59", features = ["derive"] }
